rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /automation_logs/{log} {
      allow read, write: if request.auth != null;
    }
  }
}rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /automation_logs/{document} {
      allow read, write: if request.auth != null;
    }
  }
}